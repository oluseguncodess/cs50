#include "cs50.h"
#include <stdio.h>

int main (int argc, string argv[]) {
    if (argc == 2) {
        printf("hello, %s\n", argv[0]);
    }
}

// argc refers to the number of arguments in the terminal. and argv[] refers to the index of the argument(s). 